import math

a = float(input('Digite um valor de a: '))
b = float(input('Digite um valor de a: '))
b = float(input('Digite um valor de a: '))

delta = b ** 2 - 4 * a * c

raiz1 = (-b + math.sqrt(delta) / (2 * a)
raiz2 = (-b - math.sqrt(delta) / (2 * a)

print('A primeira raiz é ', raiz1)
print('A segunda raiz é ', raiz2)